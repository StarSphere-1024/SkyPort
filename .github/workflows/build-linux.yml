name: Build Linux

on:
  workflow_call:
    inputs:
      app_version:
        required: true
        type: string
      retention-days:
        required: false
        type: number
        default: 7

permissions:
  contents: read

env:
  FLUTTER_CHANNEL: stable

jobs:
  linux:
    name: Linux Packages
    runs-on: ${{ matrix.runner }}
    strategy:
      fail-fast: false
      matrix:
        include:
          - arch: x64
            runner: ubuntu-22.04
            arch_name: amd64
            appimage_arch: x86_64
            ff_job: linux-deb
            package_type: deb
          - arch: x64
            runner: ubuntu-22.04
            arch_name: amd64
            appimage_arch: x86_64
            ff_job: linux-rpm
            package_type: rpm
          - arch: x64
            runner: ubuntu-22.04
            arch_name: amd64
            appimage_arch: x86_64
            ff_job: linux-appimage
            package_type: appimage
          - arch: arm64
            runner: ubuntu-24.04-arm
            arch_name: arm64
            appimage_arch: arm_aarch64
            ff_job: linux-arm64-deb
            package_type: deb
          - arch: arm64
            runner: ubuntu-24.04-arm
            arch_name: arm64
            appimage_arch: arm_aarch64
            ff_job: linux-arm64-rpm
            package_type: rpm
          - arch: arm64
            runner: ubuntu-24.04-arm
            arch_name: arm64
            appimage_arch: arm_aarch64
            ff_job: linux-arm64-appimage
            package_type: appimage
    env:
      APP_VERSION: ${{ inputs.app_version }}
      ARCH_NAME: ${{ matrix.arch_name }}
    steps:
      - name: Checkout
        uses: actions/checkout@v7
        with:
          fetch-depth: 0

      - name: Setup Flutter (Manual for ARM64)
        if: matrix.arch == 'arm64'
        run: |
          git clone https://github.com/flutter/flutter.git -b stable $HOME/flutter --depth 1
          echo "$HOME/flutter/bin" >> $GITHUB_PATH
          echo "$HOME/flutter/bin/cache/dart-sdk/bin" >> $GITHUB_PATH
          echo "$HOME/.pub-cache/bin" >> $GITHUB_PATH

      - name: Verify Flutter (ARM64)
        if: matrix.arch == 'arm64'
        run: |
          which flutter || echo "flutter not in PATH"
          ls -la $HOME/flutter/bin/flutter
          $HOME/flutter/bin/flutter --version
          $HOME/flutter/bin/dart --version

      - name: Setup Flutter (Standard for x64)
        if: matrix.arch != 'arm64'
        uses: subosito/flutter-action@v2
        with:
          channel: ${{ env.FLUTTER_CHANNEL }}
          cache: true
          architecture: x64

      - name: System dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y clang cmake ninja-build pkg-config libgtk-3-dev liblzma-dev libstdc++-12-dev patchelf

      - name: Install RPM build tools
        if: matrix.package_type == 'rpm'
        run: sudo apt-get install -y rpm

      - name: Install AppImage tool (appimagetool)
        if: matrix.package_type == 'appimage'
        run: |
          sudo apt-get install -y libfuse2 || echo "libfuse2 already present"
          # 根据架构选择下载链接
          if [ "${{ matrix.arch }}" = "arm64" ]; then
            URL="https://github.com/AppImage/AppImageKit/releases/download/continuous/appimagetool-aarch64.AppImage"
          else
            URL="https://github.com/AppImage/AppImageKit/releases/download/continuous/appimagetool-x86_64.AppImage"
          fi
          curl -L -o appimagetool.AppImage "$URL"
          chmod +x appimagetool.AppImage
          sudo mv appimagetool.AppImage /usr/local/bin/appimagetool
          appimagetool --version || echo "appimagetool installed"

      - name: Activate fastforge
        run: dart pub global activate fastforge

      - name: Verify AppImage patch script
        if: matrix.package_type == 'appimage'
        run: python3 .github/scripts/test_patch_fastforge_appimage.py

      - name: Patch fastforge AppImage generator
        if: matrix.package_type == 'appimage'
        run: python3 .github/scripts/patch_fastforge_appimage.py

      - name: Download Dart/Flutter deps
        working-directory: SkyPort
        run: flutter pub get

      - name: Build (${{ matrix.ff_job }})
        working-directory: SkyPort
        env:
          ARCH: ${{ matrix.appimage_arch }}
        run: fastforge release --name release --jobs ${{ matrix.ff_job }}

      - name: List dist
        working-directory: SkyPort
        run: ls -R dist || echo "dist missing"

      - name: Normalize deb filename
        if: matrix.package_type == 'deb'
        working-directory: SkyPort
        run: |
          mkdir -p dist/standard
          DEB_FILE=$(ls dist/release/*/*.deb 2>/dev/null | head -n 1)
          if [ -z "$DEB_FILE" ]; then
            echo "No .deb file found under dist/release/*/*.deb" >&2
            exit 1
          fi
          TARGET="dist/standard/SkyPort-v${APP_VERSION}-linux-${ARCH_NAME}.deb"
          cp "$DEB_FILE" "$TARGET"
          echo "Copied $DEB_FILE -> $TARGET"

      - name: Normalize RPM filename
        if: matrix.package_type == 'rpm'
        working-directory: SkyPort
        run: |
          mkdir -p dist/standard
          RPM_FILE=$(ls dist/release/*/*.rpm 2>/dev/null | head -n 1)
          if [ -z "$RPM_FILE" ]; then
            echo "No .rpm file found under dist/release/*/*.rpm" >&2
            exit 1
          fi
          TARGET="dist/standard/SkyPort-v${APP_VERSION}-linux-${ARCH_NAME}.rpm"
          cp "$RPM_FILE" "$TARGET"
          echo "Copied $RPM_FILE -> $TARGET"

      - name: Normalize AppImage filename
        if: matrix.package_type == 'appimage'
        working-directory: SkyPort
        run: |
          mkdir -p dist/standard
          APPIMAGE_FILE=$(ls dist/release/*/*.AppImage 2>/dev/null | head -n 1)
          if [ -z "$APPIMAGE_FILE" ]; then
            echo "No .AppImage file found under dist/release/*/*.AppImage" >&2
            exit 1
          fi
          TARGET="dist/standard/SkyPort-v${APP_VERSION}-linux-${ARCH_NAME}.AppImage"
          cp "$APPIMAGE_FILE" "$TARGET"
          echo "Copied $APPIMAGE_FILE -> $TARGET"

      - name: Upload deb artifact
        if: matrix.package_type == 'deb'
        uses: actions/upload-artifact@v7
        with:
          name: SkyPort-v${{ env.APP_VERSION }}-linux-${{ matrix.arch_name }}-deb
          path: SkyPort/dist/standard/SkyPort-v${{ env.APP_VERSION }}-linux-${{ matrix.arch_name }}.deb
          retention-days: ${{ inputs.retention-days }}

      - name: Upload RPM artifact
        if: matrix.package_type == 'rpm'
        uses: actions/upload-artifact@v7
        with:
          name: SkyPort-v${{ env.APP_VERSION }}-linux-${{ matrix.arch_name }}-rpm
          path: SkyPort/dist/standard/SkyPort-v${{ env.APP_VERSION }}-linux-${{ matrix.arch_name }}.rpm
          retention-days: ${{ inputs.retention-days }}

      - name: Upload AppImage artifact
        if: matrix.package_type == 'appimage'
        uses: actions/upload-artifact@v7
        with:
          name: SkyPort-v${{ env.APP_VERSION }}-linux-${{ matrix.arch_name }}-appimage
          path: SkyPort/dist/standard/SkyPort-v${{ env.APP_VERSION }}-linux-${{ matrix.arch_name }}.AppImage
          retention-days: ${{ inputs.retention-days }}
